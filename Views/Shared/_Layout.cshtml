<!DOCTYPE html>
<html lang="bs">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Diplomski</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    @if (User.Identity?.IsAuthenticated == true)
    {
        <nav class="navbar navbar-expand-lg navbar-dark autocare-zaglavlje">
            <div class="container">
                <a class="navbar-brand" asp-controller="Dashboard" asp-action="Index">AutoCare</a>
                <div class="navbar-nav ms-auto">
                    <a class="nav-link" asp-controller="Dashboard" asp-action="Index">Početna</a>
                    <a class="nav-link" asp-controller="Vozila" asp-action="Index">Vozila</a>
                    <a class="nav-link" asp-controller="Servisi" asp-action="Index">Servisi</a>
                    <a class="nav-link" asp-controller="Gorivo" asp-action="Index">Gorivo</a>
                    <a class="nav-link" asp-controller="Profil" asp-action="Index">Profil</a>
                    <a class="nav-link" asp-controller="Auth" asp-action="Odjava">Odjava</a>
                </div>
            </div>
        </nav>
    }
    <main>
        @RenderBody()
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
